# This file is managed by Ansible, all changes will be lost.
{% set ipv4_host = item.address | ipv4('host/prefix') %}
Name={{ item.name }}
Address={{ item.bind }}
Port={{ item.port }}
Compression={{ item.compression }}
Subnet={{ ipv4_host | ipaddr('address') }}

{{ item.public_key }}
